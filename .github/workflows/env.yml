name: Environment variables demo

on:
  workflow_dispatch:

env:
  APP_NAME: hello-github-actions
  ENV: dev

jobs:
  demo:
     runs-on: ubuntu-latest
     steps:
       - echo "Application: $APP_NAME"
       - echo "Environment: $ENV"